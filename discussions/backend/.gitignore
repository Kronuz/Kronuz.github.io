# Never commit the OAuth client secret, the venv, or the TLS private key/cert.
.env
.venv/
certs/
__pycache__/
*.log
# Python packaging build artifacts (pyproject.toml: pip install . / build).
build/
*.egg-info/
# Persisted store (SQLite + WAL sidecars).
discussions.db
discussions.db-wal
discussions.db-shm
